# Example environment variables
# Copy to .env and fill with your secrets locally. Do NOT commit .env.

# PUBLIC_ variables can be exposed to the client (Vite convention)
# Example:
# VITE_PUBLIC_API_BASE=https://api.example.com

# Server-only variables (do not prefix with VITE_PUBLIC_)
# Example:
# API_SECRET=replace-me
